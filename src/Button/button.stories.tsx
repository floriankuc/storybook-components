import React from 'react';
import { Button } from './';

export const Primary = (): React.ReactElement => <Button type="primary">yoo</Button>;

export default {
  title: 'Button',
  component: Button
};
